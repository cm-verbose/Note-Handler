generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model User {
  id       String   @id @unique @default(cuid())
  email    String   @unique
  password String
  created  DateTime @default(now())
}
